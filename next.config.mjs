/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "",
  images: {
    domains: [
      "img.icons8.com",
      "raw.githubusercontent.com",
      "www.google.com",
    ],
  },
};

export default nextConfig;
